-- V1__init.sql
-- This is the initial Flyway migration script for the Spring Boot Template.
-- Future database tables (like Users, Roles, etc.) will be created in subsequent scripts (e.g., V2__create_users_table.sql).

SELECT 1;